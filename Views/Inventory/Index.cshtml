@model IEnumerable<ERPConnect.Models.InventoryItem>

@{
    ViewData["Title"] = "Inventory List";
}

<h1>@ViewData["Title"] (@Model.Count() items)</h1>

<table class="table">
    <thead>
        <tr>
            <th>Product ID</th>
            <th>Quantity</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.ProductId</td>
            <td>@item.Quantity</td>
        </tr>
    }
    </tbody>
</table>

<a asp-action="Create" class="btn btn-primary">Add New Inventory Item</a>
